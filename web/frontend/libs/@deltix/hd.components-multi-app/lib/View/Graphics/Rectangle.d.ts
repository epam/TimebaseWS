/// <reference types="react" />
export interface IRectangleProps {
    color: number;
    alpha: number;
    width: number;
    height: number;
}
export declare const Rectangle: ({ color, alpha, height, width, }: IRectangleProps) => JSX.Element;
