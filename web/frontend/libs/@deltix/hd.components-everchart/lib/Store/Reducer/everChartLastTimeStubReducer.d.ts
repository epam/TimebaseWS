import { everChartLastTimeStubAction } from '../Actions/everChartActions';
import { IEverChartState } from '../everChartState';
export declare const everChartLastTimeStubReducer: (state: IEverChartState, { payload: { lastTimeStub, animate }, }: ReturnType<typeof everChartLastTimeStubAction>) => IEverChartState;
