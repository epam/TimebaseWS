import { IEverChartAnimationState } from '../everChartState';
export declare const DEFAULT_ANIMATION_DURATION = 600;
export declare const createAnimationState: (init: number, duration: number, easing: string) => IEverChartAnimationState;
